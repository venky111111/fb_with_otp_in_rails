<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Twitter</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background-color: #f4f4f4;
      margin: 0;
      padding: 0;
    }

    header {
      background-color: #333;
      color: #fff;
      padding: 10px;
      text-align: center;
    }

    nav {
      display: flex;
      justify-content: center;
      background-color: #555;
      padding: 10px;
    }

    nav a {
      color: #fff;
      margin: 0 15px;
      text-decoration: none;
    }

    .user-section {
      text-align: center;
      margin-top: 20px;
    }

    .sign-in-button,
    .sign-out-button {
      display: inline-block;
      padding: 10px 20px;
      margin-top: 10px;
      font-size: 16px;
      text-align: center;
      text-decoration: none;
      background-color: #3498db;
      color: #ffffff;
      border-radius: 5px;
      transition: background-color 0.3s ease;
    }

    .sign-out-button {
      background-color: #e74c3c;
    }

    .sign-in-button:hover,
    .sign-out-button:hover {
      background-color: #2980b9;
    }

   
  </style>
</head>
<body>

  <header>
    <h1>Fb</h1>
  </header>

  <nav>
    <a href="#">Home</a>
    <a href="#">About</a>
    <a href="#">Contact</a>
    <% if user_signed_in? %>
    <%= link_to 'Sign out', destroy_user_session_path, data: { turbo_method: :delete }, class: '' %>
    <% else %>
    <%= link_to 'Sign in', new_user_session_path, class: '' %>
    <%= link_to 'Sign up', new_user_registration_path, class: '' %>

    <% end %> 
  </nav>

  <div class="user-section">
    <% if user_signed_in? %>
      <h1>Welcome Home, <%= current_user.email %></h1>
      
      <%= link_to 'Sign out', destroy_user_session_path, data: { turbo_method: :delete }, class: 'sign-out-button' %>
    <% else %>
      <h1>Home Login</h1>
      <%= link_to 'Sign in', new_user_session_path, class: 'sign-in-button' %>
    <% end %>
  </div>

  
</body>
</html>